[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "intellidbgkit"
version = "0.1.0"
description = "PI core debug hub for embedded trace analysis"
requires-python = ">=3.12"

[project.scripts]
idk = "src.cli.main:main"
